global:
  scrape_interval: 5s
  evaluation_interval: 5s

scrape_configs:
  - job_name: 'oxia-benchmark'
    static_configs:
      # Add additional clients here for distributed runs.
      - targets:
          - 'host.docker.internal:8080'
        labels:
          instance_id: 'local-1'
      # - targets: ['host2:8080']
      #   labels:
      #     instance_id: 'remote-2'
