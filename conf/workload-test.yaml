exitWhenFinish: false
metadata:
  serverNum: 1
items:
  # insert-only
  - readRatio: 0.0
    keyspaceSize: 100000    # 100k keys
    keyDistribution: order
    valueSize: 64
    targetRate: 40000
    duration: 60s
    parallelism: 16
