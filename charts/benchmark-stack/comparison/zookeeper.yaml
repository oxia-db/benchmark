# ZooKeeper (bitnami subchart), single-node, sized for a real cluster (3 CPU / 6Gi). 3 worker pods.
zookeeper:
  enabled: true
  replicaCount: 1
  # ZooKeeper keeps every znode in the JVM heap. bitnami defaults the heap to 1024MB regardless
  # of the container limit, which the YCSB 1M keyspace overflows (Java OOM -> CrashLoopBackOff).
  # Raise the heap and give the container headroom above it for non-heap (metaspace, netty
  # direct buffers) + OS.
  heapSize: 3072
  persistence:
    enabled: true
    size: 2Gi
  image:
    repository: bitnamilegacy/zookeeper
  resources:
    requests:
      cpu: "3"
      memory: 6Gi
    limits:
      cpu: "3"
      memory: 6Gi

workers:
  - name: zookeeper-worker
    parallelism: 3
    cpu: "300m"
    memory: 384Mi
    driverConfig:
      driver: zookeeper
      config:
        servers:
          - "bench-zookeeper:2181"
