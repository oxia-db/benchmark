# HashiCorp Consul (KV), single-server, sized for a real cluster (3 CPU / 4Gi). 3 worker pods.
consul:
  enabled: true
  server:
    replicas: 1
    storage: 2Gi
    resources:
      requests:
        cpu: "3"
        memory: 4Gi
      limits:
        cpu: "3"
        memory: 4Gi
  client:
    enabled: false
  connectInject:
    enabled: false
  ui:
    enabled: false
  dns:
    enabled: false

workers:
  - name: consul-worker
    parallelism: 3
    cpu: "300m"
    memory: 384Mi
    # HashiCorp's chart names the server StatefulSet <release>-consul-server.
    waitForStsName: consul-server
    driverConfig:
      driver: consul
      config:
        address: "bench-consul-server:8500"
